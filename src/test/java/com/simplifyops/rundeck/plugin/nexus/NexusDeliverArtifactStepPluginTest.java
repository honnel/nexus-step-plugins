package com.simplifyops.rundeck.plugin.nexus;


import com.dtolabs.rundeck.core.common.INodeSet;
import com.dtolabs.rundeck.core.common.NodeSetImpl;
import com.dtolabs.rundeck.core.execution.workflow.steps.StepException;
import com.dtolabs.rundeck.plugins.step.PluginStepContext;
import org.junit.Test;

import java.util.HashMap;
import java.util.Map;

import static org.mockito.Mockito.mock;
import static org.mockito.Mockito.when;

/**
 * @author Johannes Graf - graf@synyx.de
 */
public class NexusDeliverArtifactStepPluginTest {

    @Test
    public void testDeliverArtifact() throws StepException {
/*        NexusDeliverArtifactStepPlugin sut = new NexusDeliverArtifactStepPlugin("com.simplifyops.rundeck.plugin", "nexus", "1.0.1", "jar", null, "releases", "/tmp", false, "http://localhost:8080", "john.doe", "123456");


        PluginStepContext context = mock(PluginStepContext.class);

        INodeSet t = new NodeSetImpl();
        when(context.getNodes()).thenReturn(t);

        Map<String, Object> configuration = new HashMap();


        sut.executeStep(context, configuration);*/
    }
}
